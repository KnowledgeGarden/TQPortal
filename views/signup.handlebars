
<div class="col-sm-6 col-sm-offset-3">

	<h1></span> Signup</h1>
  {{#if invitationOnly}}
  <h3>Signup is by invitation only: requires your email to be on the invitation list.</h3>
  {{/if}}
  <form action="/validate" method="post">
      <div class="form-group">
      <label>Handle must be unique. Validate Handle here. If chosen value is available, it will return in the Handle field below.</label>
      <input type="text" class="form-control" name="vhandle">
      <button type="submit" class="btn btn-warning btn-lg">Validate</button>
    </div>
  
  </form>
  <hr>
	<form action="/signup" method="post">
		<div class="form-group">
			<label>Email</label>
			<input type="text" class="form-control" name="email">
		</div>
		<div class="form-group">
			<label>Full Name</label>
			<input type="text" class="form-control" name="fullname">
		</div>
		<div class="form-group">
			<label>Handle</label>
			<input type="text" class="form-control" name="handle" value="{{ hndl }}">
		</div>
		<div class="form-group">
			<label>Avatar</label>
			<input type="text" class="form-control" name="avatar">
		</div>
		<div class="form-group">
			<label>Home Page</label>
			<input type="text" class="form-control" name="homepage">
		</div>
		<div class="form-group">
			<label>Password</label>
			<input type="password" class="form-control" name="password">
		</div>

		<button type="submit" class="btn btn-warning btn-lg">Signup</button>
	</form>

	<hr>

	<p>Already have an account? <a href="/login">Login</a></p>
	<p>Or go <a href="/">home</a>.</p>

</div>
