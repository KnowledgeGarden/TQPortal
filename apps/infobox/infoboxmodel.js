/**
 * InfoBoxModel
 * This model should have the ability to read an InfoBox schema (JSON) and
 * paint an HTML form from it, then handle the InfoBox data
 */
var types = require('../../node_modules/tqtopicmap/lib/types')
, icons = require('../../node_modules/tqtopicmap/lib/icons')
, properties = require('../../node_modules/tqtopicmap/lib/properties')

  , constants = require('../../core/constants')
;

var InfoBoxModel = function(environment) {
	
};
